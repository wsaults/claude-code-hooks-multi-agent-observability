{"_type":"issue","id":"dash-1io","title":"DASH-4: End-to-end verification of Phase 1","description":"\n## Goal\n\nProve the pipeline works before pointing real workers at it.\n\n## Scope\n\n- Boot server + client\n- Send synthetic event sequences simulating 2–3 workers across different orgs/projects:\n  - normal working flow (SessionStart → tools → Stop)\n  - blocked flow (Notification permission_prompt → needs-you flag)\n  - resume flow (same worktree, new session_id → same card)\n- Verify board renders cards, statuses transition, drill-down filters correctly\n- Verify real hooks from one actual Claude Code session in a test worktree\n\n## Done when\n\nAll flows above pass; README updated with setup steps for new worktrees.","status":"open","priority":1,"issue_type":"task","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:14Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-1"],"dependencies":[{"issue_id":"dash-1io","depends_on_id":"dash-zt5","type":"blocks","created_at":"2026-06-07T14:53:25Z","created_by":"Will Saults","metadata":"{}"},{"issue_id":"dash-1io","depends_on_id":"dash-b9r","type":"blocks","created_at":"2026-06-07T14:53:25Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"dash-b9r","title":"DASH-3: Client: Board view homepage","description":"\n## Goal\n\nFlip the primary view from event timeline to a per-worker board — the \"at a glance\" answer to \"what's going on and who needs me.\"\n\n## Scope\n\n- New default homepage: one card per worker, grouped org → project\n- Card contents: title (fallback: repo + branch), lifecycle badge, \"⚠️ needs you\" flag, ticket label, last activity (relative), model name\n- `useWorkers.ts` composable: initial fetch from `GET /workers` + live updates from WS\n- Existing timeline demoted to drill-down: click a card → timeline filtered to that worker\n- View toggle in header (Board / Timeline)\n- Stale workers (no events 30+ min) visually muted\n\n## Done when\n\n- Multiple concurrent workers render as separate cards with live status\n- Needs-you flag appears on Notification/PermissionRequest and clears when work resumes\n- Card click opens the filtered timeline","status":"open","priority":1,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:13Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-1"],"dependencies":[{"issue_id":"dash-b9r","depends_on_id":"dash-zt5","type":"blocks","created_at":"2026-06-07T14:53:24Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"dash-zt5","title":"DASH-2: Server: schema, workers table, lifecycle status","description":"\n## Goal\n\nServer understands workers as first-class entities and derives lifecycle status from the event stream.\n\n## Scope\n\n- Extend `events` table with worker fields (additive migration, same pattern as existing column checks in `db.ts`)\n- New `workers` table: worker_id (PK), org, project, host, branch, ticket, title, cwd, last session_id, last_event_type, last_activity, status, needs_attention\n- Upsert worker row on every incoming event\n- Lifecycle mapping:\n  - SessionStart / UserPromptSubmit / PreToolUse / PostToolUse / Subagent* / PreCompact → `working`\n  - Stop → `idle` (finished responding, may be waiting on review)\n  - Notification / PermissionRequest → `blocked` + needs_attention flag\n  - SessionEnd → `ended`\n  - needs_attention clears on next UserPromptSubmit or tool use\n- `GET /workers` endpoint\n- Broadcast worker upserts over existing WebSocket (`{type: 'worker', data}`); include workers in the initial WS payload\n\n## Done when\n\n- Posting a synthetic event sequence produces correct worker rows and status transitions\n- WS clients receive worker updates in real time\n\n## Open question\n\nStale rule: gray out workers with no events for 30+ min (SessionEnd doesn't always fire). Decide threshold during DASH-3.","status":"open","priority":1,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:13Z","created_by":"Will Saults","updated_at":"2026-06-07T14:57:40Z","labels":["phase-1"],"dependencies":[{"issue_id":"dash-zt5","depends_on_id":"dash-xzl","type":"blocks","created_at":"2026-06-07T14:53:24Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"dash-xzl","title":"DASH-1: Worker identity layer in hooks","description":"\n## Goal\n\nEvery hook event carries a stable worker identity so the dashboard can key cards on workers, not ephemeral sessions.\n\n## Scope\n\n- `identity.py` in `.claude/hooks/utils/`: resolves worker_id, org, project, host, branch, ticket, cwd\n- Stable `worker_id`: UUID written to `\u003cworktree\u003e/.claude/worker-id` on first event (survives resumes; `session_id` is ephemeral)\n- Hybrid derivation: org = git remote owner, project = repo name, ticket = branch regex (`ABC-123`)\n- `.claude/dashboard.json` overrides any derived field: `{\"org\", \"project\", \"ticket\", \"title\"}`\n- `send_event.py` attaches the result as a `worker` object on every event\n- Fail-safe: identity errors must never block event delivery\n\n## Done when\n\n- Events posted from any git worktree include a populated `worker` object\n- Same worker_id across session resumes in the same worktree\n\n## Notes\n\nPrototyped in scratch; derivation validated against the fork's own repo (org `wsaults` from remote).","status":"open","priority":1,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:12Z","created_by":"Will Saults","updated_at":"2026-06-07T20:00:09Z","labels":["enhancement","phase-1","ready-for-agent"],"comments":[{"id":"019ea3df-d24b-7abe-8984-6ee92371d8e8","issue_id":"dash-xzl","author":"Will Saults","text":"\u003e *This was generated by AI during triage.*\n\n## Agent Brief\n\n**Category:** enhancement\n**Summary:** Give every hook event a stable per-worktree **Worker** identity so the dashboard keys cards on Workers, not ephemeral Sessions.\n\n**Current behavior:**\nThe canonical hook dispatcher (`send_event.py` in the repo-root `.claude/hooks/`) posts each event with `source_app`, `session_id`, `hook_event_type`, `payload`, `timestamp`, `model_name`. `model_name` is derived by a `utils/` module (`model_extractor.py`) that caches per session. There is no Worker concept — the dashboard can only group by the ephemeral `session_id`, so a resumed agent looks like a brand-new entity.\n\n**Desired behavior:**\nA new identity util (sibling to `model_extractor.py` in the root hooks `utils/`) derives a `worker` object that the dispatcher attaches to every event as a top-level `worker` field. See `CONTEXT.md` for the Worker / Session / Source App vocabulary. The object carries exactly these identity fields (the server owns lifecycle/status, not the hook): `worker_id, org, project, host, branch, ticket, cwd, title`.\n\nDerivation rules:\n- `worker_id`: random UUIDv4, persisted to `\u003cworktree\u003e/.claude/worker-id` on first event and reused (survives Session resumes; `session_id` does not). The module must ensure this file is git-ignored on creation, so a committed `.claude/` can't clone the id across machines and merge two distinct Workers.\n- Overrides: `.claude/dashboard.json`, if present, overrides any of `{org, project, ticket, title}`.\n- Caching: stable fields (`worker_id, org, project, host`) computed once and cached in a git-ignored cache file (no `git remote` lookup per event); volatile fields (`branch, ticket, title, cwd`) re-derived each event.\n\nFail-safe:\n- Derivation never blocks or corrupts delivery — errors caught, logged to stderr only, never propagated.\n- Underivable field -\u003e explicit `null` (others unaffected).\n- If `worker_id` itself can't be established -\u003e omit the `worker` object, still deliver the event (no synthetic id).\n\n**Key interfaces:**\n- New identity module in root hooks `utils/` exposing a function returning the `worker` object (or omitted on total failure), mirroring `model_extractor`.\n- Event payload gains top-level `worker` with the 8 fields (value `null` when underivable), alongside existing fields.\n- `.claude/dashboard.json` shape: `{ \"org\"?, \"project\"?, \"ticket\"?, \"title\"? }` — present keys override derived values.\n\n**Acceptance criteria:**\n- [ ] Events from any git worktree include a populated `worker` object (8 fields).\n- [ ] Same `worker_id` across Session resumes in one worktree (new `session_id`, same `worker_id`).\n- [ ] `worker_id` is a persisted UUIDv4 and is git-ignored (never committed).\n- [ ] `org/project/ticket/title` are `null` (not missing) when underivable; rest still emitted.\n- [ ] `.claude/dashboard.json` values override the corresponding derived fields.\n- [ ] `git remote`-derived fields aren't recomputed every event; `branch/ticket/title` reflect mid-session changes.\n- [ ] A forced identity error never blocks delivery; `worker` is omitted only when `worker_id` can't be established.\n\n**Out of scope:**\n- Server `workers` table, lifecycle/status, `GET /workers`, WS broadcast -\u003e DASH-2 (`dash-zt5`, which this blocks).\n- Board view UI -\u003e DASH-3.\n- Propagating identity into `apps/demo-cc-agent/` sample hooks — a separate follow-up.","created_at":"2026-06-07T20:56:55Z"}],"dependency_count":0,"dependent_count":2,"comment_count":1}
{"_type":"issue","id":"dash-zeg","title":"DASH-9: Linear + GitHub enrichment on cards","description":"\nPull live Linear ticket state and GitHub PR review status via API/MCP, keyed off the ticket ref and branch already on each worker card. Server-side polling with cache; card shows ticket status + PR checks/review state.","status":"open","priority":2,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:16Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-5"],"dependencies":[{"issue_id":"dash-zeg","depends_on_id":"dash-b9r","type":"blocks","created_at":"2026-06-07T14:53:28Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dash-zi8","title":"DASH-8: Centralize: agents dial out over Tailscale","description":"\nSplit agent ↔ central server. Agents on each machine dial out over a persistent WebSocket (Tailscale network), so no inbound ports on worker machines. Dashboard groups by org → project → host; control commands route back down the agent's WS.","status":"open","priority":2,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:16Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-4"],"dependencies":[{"issue_id":"dash-zi8","depends_on_id":"dash-5hk","type":"blocks","created_at":"2026-06-07T14:53:27Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dash-8aa","title":"DASH-7: tmux control layer (opt-in per worker)","description":"\nOpt-in: launch a worker in a named detached tmux session to get dashboard actions (answer prompt, nudge) via `tmux send-keys`. Everyday Warp-tab workers stay read-only — Warp and tmux don't mix well, so control is reserved for workers you explicitly want to drive remotely.","status":"open","priority":2,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:15Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-3"],"dependencies":[{"issue_id":"dash-8aa","depends_on_id":"dash-1io","type":"blocks","created_at":"2026-06-07T14:53:27Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dash-f8t","title":"DASH-6: Semantic status self-report (status.json)","description":"\nWorkers self-report `title` and `next_steps` into `\u003cworktree\u003e/.claude/status.json` — via a Stop-hook transcript summary or a slash command. Identity layer already reads this file if present, so this ticket is about producing it reliably. \"Questions\" field cut per earlier decision.","status":"open","priority":2,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:15Z","created_by":"Will Saults","updated_at":"2026-06-07T19:57:18Z","labels":["phase-2"],"dependencies":[{"issue_id":"dash-f8t","depends_on_id":"dash-b9r","type":"blocks","created_at":"2026-06-07T14:53:26Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"dash-5hk","title":"DASH-5: Per-machine agent (forwarding + offline queue)","description":"\nThin local process: hooks POST to localhost agent → agent enriches (host context) and forwards upstream with an offline buffer. Upstream URL is config: localhost today, central server in Phase 4 with zero hook changes. Deferred from Phase 1 since it's near-passthrough on a single machine; its dial-out WebSocket is what later carries control commands back down (DASH-7/8).","status":"open","priority":2,"issue_type":"feature","owner":"william@mudflapinc.com","created_at":"2026-06-07T19:53:14Z","created_by":"Will Saults","updated_at":"2026-06-07T14:57:40Z","labels":["phase-4-prep"],"dependencies":[{"issue_id":"dash-5hk","depends_on_id":"dash-xzl","type":"blocks","created_at":"2026-06-07T14:53:26Z","created_by":"Will Saults","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
